import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello, World!")
            .font(.system(size: 20, weight: .bold)) // Applying a system font with specific size and weight
    }
}
